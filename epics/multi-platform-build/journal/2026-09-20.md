# 2026-09-20, link 1: M1, the portable build proven on Linux

Link 1 of the chain (`mpb-1`, Claude `claude-fable-5-1` at effort high), started from a clean tree
at `b04ceac`, the commit in which the owner narrowed M1's validation to the checks the change
needs: a hash comparison for the guards, `./run check` for the version bump, and the packaging
commands for the packaging scripts. The reviewer window `mpb-review` was alive but not yet
registered when this link started; it registered on its own while the first build ran.

## What changed

`CMakeLists.txt` sets the macOS defaults before `project()`: `CMAKE_OSX_ARCHITECTURES` to
`arm64;x86_64` and `CMAKE_OSX_DEPLOYMENT_TARGET` to `11.0`, only when neither the cache nor the
environment already holds a value. The test is on `CMAKE_HOST_APPLE`, not `APPLE`: before
`project()` CMake has not identified the target system yet, so `APPLE` is empty, while
`CMAKE_HOST_APPLE` is set by CMake itself at start-up; I verified with a scratch project that on
this Linux machine `CMAKE_HOST_UNIX` is `1` and `CMAKE_HOST_APPLE` and `APPLE` are both empty at
that point. The macOS build is native, so the host is the target. The two sanitizer options are
refused with `FATAL_ERROR` when `CMAKE_SYSTEM_NAME` is not `Linux`. The warning options are chosen
by compiler: `/W4` under `MSVC`, the GCC-style set otherwise, in a `KCF_WARNING_OPTIONS` variable
that `kcf_engine` takes. `FORMATS` gains `AU`. The comment on the disabled MIDI CC emulation cited
`docs/architecture.md`, a file that no longer exists; the README does not document that behaviour
either (I grepped for it), so the comment now stands on its own and points at the REAPER `panic`
stage in the development guide. `THIRD_PARTY_NOTICES.md` cited the same dead file for the
mathematical definitions and now names the README's "How it works" section. The version is 1.4.0.

`tests/CMakeLists.txt` keeps `kcf_engine_tests` outside any guard with its own compiler-aware
warning options, and puts the three Linux-only executables, their `find_package(PkgConfig)`,
`pkg_check_modules` and `X11`, `rt`, `pthread` links inside `CMAKE_SYSTEM_NAME STREQUAL "Linux"`.

`plugin/Presets.cpp` adds the `Application Support` component under `JUCE_MAC` only, after the
`KCF_PRESET_DIR` check, which still wins on every platform. The edit sits after the file's only
assertion site, so no `__LINE__` moved.

`run` computes its default target list from `uname -s` (the three Linux-only test targets on
Linux, `KickCrafterFable_AU` on macOS, neither on Windows), appends `.exe` to the test binaries
under `MINGW*` and `MSYS*`, and gains `dist`. `DISPLAY` and `KCF_DISPLAY` keep their defaults on
every platform: `set -u` would otherwise trip on them, and exporting them is harmless where
nothing reads X11. Nothing bash 4 was added; the script already had none.

`tools/run-logged.sh` defines the `sha256` helper (`sha256sum` when it exists, `shasum -a 256`
otherwise) and computes the manifests with a `while read` loop feeding it instead of `xargs
sha256sum`. I checked that the loop yields the same hash as the old pipeline on the current tree
(both lines print hash, two spaces, path). The log stamp's nanoseconds come from `date +%N` when
the output is digits and are `000000000` otherwise, because BSD `date` prints a literal `N`; on
Linux the file names keep their exact former shape.

`tools/dist.sh` is new: platform from `uname -s` (`Linux`, `Darwin`, `MINGW*`/`MSYS*`, anything
else refused), version parsed from `project(... VERSION ...)`, the short commit appended when
`GITHUB_ACTIONS` is `true` and `GITHUB_REF_TYPE` is not `tag`, the eleven licence files in one
function, an `INSTALL.txt` per platform, `tar.gz` on Linux, `ditto -c -k --keepParent` on macOS
(Apple's own archiver for bundles, which the signing job will reuse after stapling), `7z` with a
`zip` fallback on Windows. Besides the no-argument form it offers `name`, `version`, `stage <dir>
[<from>]` and `archive <dir> <out>`, so that `package.sh` can stage the validated bundle from
`artifacts/vst3`, append its validation lines to `INSTALL.txt`, add `BUILD-RECORD.txt`, and only
then archive. The archivers run inside the staging directory with relative paths so that no
absolute POSIX path crosses into a native Windows program. The macOS `INSTALL.txt` carries a
`Signing:` line that says the bundle is unsigned and how to clear the quarantine attribute;
`tools/macos-sign.sh` (M3) will rewrite that line.

`tools/package.sh` now takes the binary archive from `dist.sh` and names its three archives
`kickcrafter-fable-<version>-...`. Neither `package-preflight.sh` nor `package-negative-controls.sh`
read the archive name (checked with `grep -n kickcrafter-fable tools/`), so they needed nothing for
the rename. I did add `run` to the archived paths of the source archive, the preflight's clean-tree
list and the negative controls' scratch copy: the guide says everything goes through `./run`, CI
will depend on it, and a source archive without it could not follow its own documentation. This is
a small widening of the seam list, recorded here for the reviewer.

`CHANGELOG.md` has the 1.4.0 entry dated from `date +%F` (2026-09-20). `CLAUDE.md` states the three
platforms and the validation asymmetry in its closed decisions, names them in its first line and
lists `./run dist` among the pieces. `docs/development.md` describes `dist.sh` next to the
packaging paragraph and adds a row for it to the what-to-run table.

## Validation

The hash comparison: with every guard, the `AU` format and the `Presets.cpp` change applied and the
version still 1.3.0, `./run build` (logs `20260920T184800...-configure-build.log` and
`20260920T184804...-build.log`, both exit 0) recompiled only `Presets.cpp.o`, relinked the shared
code, the VST3 and the tests, and recompiled the unshipped Standalone's wrapper units (its
definitions follow the format list). The relinked module hashed
`72fb25b6c3d43f6f80e8eafe78763c432699c1897bc76901567cd2a4c1668944`, identical to `vst3-so` in the
build record of `20260920T173357Z`. I had confirmed beforehand, in `JUCEUtils.cmake`, that JUCE
intersects `FORMATS` with the platform's plug-in kinds before defining any `JucePlugin_Build_*`
symbol, so `AU` adds no definition on Linux.

After the version bump and the script changes, `./run check` (gate file
`artifacts/logs/gates/20260920T190236Z-check.txt`, every step exit 0): plug-in tests 28 cases,
1166 checks, 0 failures (`20260920T191353...-plugin-tests.log`), engine tests 23 cases, 1180
checks, 0 failures (`20260920T191406...-engine-tests.log`), validator 47 passed, 0 failed
(`20260920T191412...-validator.log`), the new build record binding module
`f34045284d784b583e694cfed4a88158acddfcda9ccb4a245c0d4d3b1b3dfbaf` to manifest
`d4275a85c54e833451e2f4535e6df87984c0ecc523d889ca0809443f3a74d342`. The logs name `b04ceac` with
dirty paths, as they must before the commit; the manifest is the one the commit carries.

`./run dist` produced `artifacts/dist/kickcrafter-fable-1.4.0-linux-x86_64.tar.gz` (SHA-256
`9570b3ac...`) whose listing holds the bundle, `README.md`, `CHANGELOG.md`, `LICENSE`,
`THIRD_PARTY_NOTICES.md`, `INSTALL.txt` and the eleven licence files; I compared the licence names
against the list in `HEAD:tools/package.sh` and they are the same eleven.

Not run, on purpose and per the owner's instruction: `./run validate` and the memory passes. No
Linux code path that alters host behaviour or a lifecycle changed: the module was byte-identical
under the guards, and the version string is the only difference afterwards.

`./run package`, `./run package-controls` and `./run preflight` run after the implementation
commit, because the preflight requires the archived paths to be committed; their results follow.

## Decisions taken under uncertainty

`CMAKE_HOST_APPLE` instead of `APPLE` for the pre-`project()` defaults (explained above). `run`
added to the source archive and the preflight's list. The stale comment pointed at the guide's
`panic` stage rather than at a README section that does not exist. `DISPLAY` exported on every
platform. `dist.sh` refuses an unknown `uname -s` rather than guessing. `ditto` for the macOS zip
and `7z` for Windows, both present on the GitHub runners, with `zip` as the Windows fallback.

## After the commits

The implementation is commit `ec1ae68`. Its first `./run package` failed in the script's own final
verification, which I had left reading the old `-<rev>-` archive names (`tar` could not open
`kickcrafter-fable-ec1ae68-source.tar.gz`); the preflight had already passed and the three
archives were written, only the check after them was wrong. Commit `8b2481c` points that
verification at the version-based names. On it, `./run package` produced
`kickcrafter-fable-1.4.0-linux-x86_64.tar.gz`, `kickcrafter-fable-1.4.0-source.tar.gz`,
`kickcrafter-fable-1.4.0-evidence.tar.gz` and `SHA256SUMS`, verified the source archive (the pinned
JUCE tree accepted by `fetch-juce.sh`, the manifest `d4275a85...` recomputed from the extraction,
`run` present) and counted eleven licence files in the binary archive. `./run package-controls`
(`20260920T191622...-package-negative-controls.log`, exit 0) reported fourteen `ok` lines and no
`FAIL`: the preflight still refuses every fixture. `./run preflight` on `ec1ae68` and again after
the package run printed `preflight ok` for module `f3404528...` and manifest `d4275a85...`, with the
plug-in-test and validator logs of the check gate. The tree is clean; this journal entry and the
`tasks.md` marker are the last commit of the milestone, submitted to the reviewer as M1.

## Review m1-001: four findings, addressed in `1c2fe02`

The reviewer returned FINDINGS on `9628b8e`. One: `tests/engine_tests.cpp` uses `M_PI`, which
Microsoft's `<cmath>` defines only when `_USE_MATH_DEFINES` precedes the first include; the
engine-test target now gets that definition on the command line under `MSVC`, nothing else
changes. Two: my environment check for the deployment target read
`ENV{CMAKE_OSX_DEPLOYMENT_TARGET}`, but the variable CMake honours is `MACOSX_DEPLOYMENT_TARGET`
(the architectures one is indeed `CMAKE_OSX_ARCHITECTURES`; both are in CMake's `Help/envvar/`),
so a caller exporting it would have been overridden by the 11.0 default; the check now names the
right variable and the comment says why. Three: `dist.sh` accepted a staging with only the AU
component; it now requires `KickCrafter Fable.vst3` on every platform and the component in
addition on macOS. Four: the hash comparison, `dist`, `package` and `preflight` had no retained
log with a header and an exit sidecar, only the session's output and the files under
`artifacts/dist/`. The packaging commands of `run` (`dist`, `preflight`, `package`) now go through
`tools/run-logged.sh` like the tests, so every run leaves a log naming the commit and the manifest
with its real exit code. For the original guard comparison at 1.3.0 I preserved the session's
command output verbatim as
`artifacts/logs/runs/20260920T184804.523849290Z-43206-guard-hash-comparison.session-output.txt`
(it is a capture, not a run-logged log, and has no sidecar: the build itself is the logged part,
`...-43206-build.log`, exit 0, in which only `Presets.cpp.o` recompiles before the links). Redoing
that measurement would mean two full recompiles of the unity build (the version string is a
definition on every translation unit), which the owner's instruction does not justify; the
retained measurement below covers the CMake changes of this round in the same way.

Validation of the round, all on this machine. `./run build-chain` after the CMake edits
(record `20260920T192156Z`, plug-in tests `20260920T192749...-plugin-tests.log`, 28 cases, 1166
checks, 0 failures, exit 0): nothing recompiled, and the record's `vst3-so` is
`f34045284d784b583e694cfed4a88158acddfcda9ccb4a245c0d4d3b1b3dfbaf`, the hash of the check gate's
record `20260920T190236Z`. The logged measurement `20260920T192819...-module-hash.log` (exit 0)
prints the `vst3-so` line of the three records of the day (`72fb25b6...` at 1.3.0 before the
epic, `f3404528...` twice at 1.4.0) and the hashes of the module in `build/` and in
`artifacts/vst3`, both `f3404528...`. The engine tests and the validator were not rerun: the
engine sources and the module are unchanged, and the newest validator log still names the staged
module, which the preflight checks. Then on the committed `1c2fe02`: `./run dist`
(`20260920T192820...-dist.log`, exit 0, the listing with the bundle, the five documents and the
eleven licence files), `./run package` (`20260920T192838...-package.log`, exit 0, the three
archives and `SHA256SUMS`, the source archive verified), `./run package-controls`
(`20260920T192851...-package-negative-controls.log`, exit 0, 14 ok, 0 FAIL) and `./run preflight`
(`20260920T192902...-preflight.log`, exit 0, `preflight ok` for manifest `1aa19353...` and module
`f3404528...`). The tree is clean.
